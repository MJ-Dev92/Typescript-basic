let isDone: boolean = false;

isDone = true;

console.log(typeof isDone); // 'boolean'

let isOk: Boolean = true;

// let isNotOK: boolean = new Boolean(true); // 대문자 boolean은 사용하지 않고 보통 리터럴 방식으로 사용한다.